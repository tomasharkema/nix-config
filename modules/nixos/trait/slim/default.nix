{
  config,
  pkgs,
  lib,
  ...
}:
with lib; {
  # options.trait.slim = {
  #   enable = mkEnableOption "slim";
  # };

  # config = {
  #   services = {
  #     kmscon = {
  #       enable = false;
  #     };
  #   };
  # };
}
