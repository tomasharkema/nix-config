{lib, ...}:
with lib; {
  # options.user = with types; {
  #   name = mkOption {
  #     type = types.str;
  #     default = "tomas";
  #     description = "The name to use for the user account.";
  #   };
  # };
}
